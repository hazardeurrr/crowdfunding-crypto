const chain = '0x4' // Rinkeby -- change to 0x1 for production on Mainnet
                              // don't forget to change the switchToRinkeby -> switchToETH dans navbar

module.exports = {
  chain
}